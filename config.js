const serverConfig = {
  hostname: '127.0.0.1',
  port: 3000,
  eventEmitter: null
};

const routerConfig = {
  eventEmitter: null
};

const appsDirectory = './apps';

module.exports = {
  serverConfig,
  routerConfig,
  appsDirectory
};